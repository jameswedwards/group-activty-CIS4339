<%= form_with(model: class_assignment, local: true) do |form| %>
  <% if class_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_assignment.errors.count, "error") %> prohibited this class_assignment from being saved:</h2>

      <ul>
        <% class_assignment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :assignment_name %>
    <%= form.text_field :assignment_name %>
  </div>

  <div class="field">
    <%= form.label :due_date %>
    <%= form.date_select :due_date %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.text_field :points %>
  </div>

  <div class="field">
    <%= form.label :school_class_id %>
    <%= form.text_field :school_class_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
